export interface Detail{
    color: {
        color: string, 
        id: number
    },
    quantity: number, 
    price: number, 
    id: number, 
    size: {
        size: string,
        id: number 
    }
}